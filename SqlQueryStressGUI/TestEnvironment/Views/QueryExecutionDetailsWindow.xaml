<Window x:Class="SqlQueryStressGUI.TestEnvironment.Views.QueryExecutionDetailsWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Query Execution Details" Height="300" Width="500">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Label Grid.Row="0" Margin="10,0,0,0" Content="Execution Statistics" />
        <DataGrid
            Grid.Row="1"
            Margin="10"
            ItemsSource="{Binding ExecutionStatisticsTable}"
            AutoGenerateColumns="True"
            IsReadOnly="True"/>

        <Label Grid.Row="2" Margin="10,0,0,0" Content="Query Parameters" />
        <DataGrid Grid.Row="3" Margin="10" ItemsSource="{Binding QueryParameters}" AutoGenerateColumns="True" IsReadOnly="True"/>
    </Grid>
</Window>
