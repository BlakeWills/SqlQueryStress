<UserControl x:Class="SqlQueryStressGUI.Views.ParameterSettings.RandomNumberView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             mc:Ignorable="d" 
             d:DesignHeight="100" d:DesignWidth="600">
    <Grid Margin="10">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>
        
        <TextBlock Grid.Row="0" Grid.Column="0" Text="Min Value" />
        <TextBox Grid.Row="0" Grid.Column="1" Text="{Binding MinValue}" />

        <TextBlock Grid.Row="1" Grid.Column="0" Text="Min Value" />
        <TextBox Grid.Row="1" Grid.Column="1" Text="{Binding MinValue}" />

    </Grid>
</UserControl>
